// Package api contains end-to-end tests that hit a running server over HTTP.
// These tests require the server binary to be built and local services
// (PostgreSQL, NATS) to be running.
//
// E2E tests will be added as API endpoints are implemented.
package api

import (
	"testing"
)

// TestE2EPlaceholder is a placeholder that verifies the E2E test framework
// compiles and runs. Real E2E tests will be added when endpoints exist.
func TestE2EPlaceholder(t *testing.T) {
	t.Log("E2E test framework is set up. Actual tests pending endpoint implementation.")
}
